const num = parseFloat(prompt("Enter The Value"));
document.write("Actual Value:" + num);

const roundValue = Math.round(num);
document.write("<br/>roundValue:" + roundValue);


// Go live with html file to use output 
