      //  Current Balance
      let balance = 500;
      document.write("<br/> Current Balance" + balance + "<br/>")

      // 15 rs. Added in Current Balance
      // balance = balance + 15

      balance += 15
      document.write("<br/>New Balance" + balance + "<br/>")

      // 15 rs. deduct in Current Balance
      // balance = balance - 15
      balance -= 15
      document.write("<br/>New Balance" + balance + "<br/>")

      // 15 rs. * in Current Balance
      // balance = balance * 15
      balance *= 15
      document.write("<br/>New Balance" + balance + "<br/>")

        // 15 rs. / in Current Balance
      // balance = balance / 15
      balance /= 15
      document.write("<br/>New Balance" + balance + "<br/>")

        // 15 rs. % 0f Current Balance
      // balance = balance % 15
      balance %= 15
      document.write("<br/>New Balance" + balance + "<br/>")
